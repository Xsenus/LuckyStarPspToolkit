# Требования и фактическая приёмка

Дата аудита: **22 сентября 2026 года**. Исходная точка — 0.19.0,
коммит `9edbc38f6e56ac67fe62f598626efac786d6026d`; исправления этой проверки
включены в 0.19.1. Матрица основана на исходниках, документации передачи и
пользовательских сообщениях из полной истории разработки. Она не заменяет договор с заказчиком.

Проект реализует инструменты перевода и систему лицензирования. **Интеграция
перевода заказчика в обе игры и запуск изменённого образа на PSP пока не подтверждены.**
В переданном комплекте нет настоящих `sc.cpk`, `lt.bin`, чистого ISO и проверенной
карты глифов. Исторический анализ EBOOT не заменяет повторный прогон текущего кода.

## Исходный объём запроса

Заказчик просил консольную утилиту для **Lucky Star: Ryouou Gakuen Outousai Portable**
и **Lucky Star: Net Idol Meister**: извлечение текста в TXT либо JSON, обратную
сборку с корректным чтением игрой, интеграцию собственного перевода заказчика и
шрифт с полной кириллицей и необходимыми специальными символами. Первоначально
упоминался C++, затем владелец проекта явно выбрал реализацию целиком на C#.

Владелец дополнительно запросил документацию и XML summary методов/функций,
оптимизацию алгоритмов, проверку присланных файлов, подготовку GitHub и
автоматическую сборку релизов. Позднее добавлены выдача ключей на часы/дни/годы
или бессрочно, блокировка без действующего доступа, React-админка и отключаемый
резерв с блокировкой без обновления более семи дней. Требование публичной
публикации и установки на VPS подтверждено текущим поручением.

## Как читать статусы

- **Проверено на синтетике** — текущий C#-код реально выполнен с искусственными
  бинарными эталонами; игровые файлы в такой проверке не участвовали.
- **Реализовано** — соответствующий путь присутствует в исходниках; свежий
  целевой интеграционный прогон ещё нужно подтвердить отчётом.
- **Частично / нужны данные** — есть ограничение формата, неподтверждённый профиль
  или отсутствует необходимый вход для приёмки.
- **Историческое свидетельство** — результат прошлой версии, сохранённый отдельно.

## Игровая часть

| Требование | Где реализовано и что проверяется | Статус и границы |
|---|---|---|
| Единый C#/.NET инструмент и понятные команды | `src/LuckyStarPspToolkit.Cli`, [CLI](CLI_REFERENCE.md) | Core/Formats работают на штатном .NET 9; найденные несовместимые перегрузки строк исправлены в 0.19.1. Полный release report учитывается отдельно. |
| Определить игру и ревизию до правок | `Core/Sfo.cs`, `Core/Inspection.cs`, профили RGO/NIM | Проверено на синтетике; новый raw-byte прогон SFO заказчика требует оригинала. |
| Расшифровать поддерживаемый EBOOT с проверкой целостности | `Core/PspPrx.cs`, `Elf.cs`, `SelfDiagnostics.cs`; Type-2 `0xD91613F0`, SHA-1, KIRK CMAC, ELF32 MIPS | Core self-tests прошли на .NET 9, включая 81 независимый CMAC-вектор. Другие PRX-ревизии не объявляются поддержанными. |
| Безопасно применить RGO VWF и не повредить неизвестный EBOOT | `RgoVwfPatchProfile.cs`, [описание](EBOOT_VWF_PATCH_RU.md); полные SHA-256/preconditions, идемпотентность | Проверено на синтетике; 133 слова профиля исторически сверены с EBOOT заказчика. Происхождение таблицы отдельно указано в [notices](../THIRD_PARTY_NOTICES.md). |
| Пересчитать размеры сценариев и heap после роста текста | `Workspace/EbootSizePatchPlan.cs`, `Core/RgoProfile.cs` | Проверено на синтетике; требуется проверить реальный EBOOT вместе с новым CPK. Выход — расшифрованный ELF, не подписанный Sony retail PRX. |
| Читать, извлекать и пересобирать CPK/ITOC/@UTF | `Formats/Cri`, [алгоритм CPK](CPK_CODEC_RU.md) | Проверено на синтетике: no-op побайтовый, переходы DataL/DataH, неизвестные совместимые metadata сохраняются. Неподдержанные индексы и несовместимые схемы отклоняются. |
| Обрабатывать CRILAYLA | `Formats/Cri/CrilaylaCodec.cs` | Декодирование и сохранение исходных сжатых payload проверены на синтетике. Новые изменённые сценарии записываются без сжатия; encoder не реализован. |
| Выгрузить диалоги, имена и варианты в редактируемый JSON | `Formats/Scripts`, `TranslationWorkspaceService.cs` | Проверено на синтетике. Настоящие диалоги нельзя принять без `sc.cpk` и карты глифов. |
| Внести русский текст и пересчитать переходы/контрольные суммы | `LuckyStarScript.Build.cs`, `ScriptOffsetMap.cs`, [алгоритм](SCRIPT_CODEC_RU.md) | Проверено на синтетике: рост текста, границы, смещения и безопасный отказ для неоднозначного перехода. Игровые ветки требуют отдельного прохождения. |
| Сохранить нетронутые данные и оригиналы при ошибке | `Common/AtomicFile.cs`, строгий JSON, workspace и release guards | Проверено на синтетике, включая атомарную запись и откат. Набор разных файлов не объявляется одной crash-atomic транзакцией. |
| LT/BDF, кириллица и Ё/ё | `Formats/Fonts`, `Text/GlyphMap.cs` | Синтетические LT/BDF и покрытие 66/66 букв прошли. Готовый художественный шрифт и качество отображения в игре не подтверждены; нужна лицензированная гарнитура и настоящий `lt.bin`. |
| Собрать минимальный комплект из ISO | `PspAssetCollector.cs`, [сбор данных](USAGE_RU.md) | Проверено на синтетике: пути, манифест, размеры, хэши; нужен принадлежащий заказчику чистый ISO. |
| Создать отдельный изменённый ISO, сохранив остальные данные | `Iso9660Image.cs`, `Iso9660Rebuilder.cs`, [описание](ISO_REBUILD_RU.md) | Синтетические endian/range/multi-extent/rebuild проверки прошли; загрузка настоящего ISO в PPSSPP/PSP ещё не проверена. |
| Ограничить память, размеры, переполнения и повреждённый ввод | `FileLimits.cs`, CPK/UTF/script/ISO parsers и regression self-tests | Проверено на синтетике и граничных наборах. Производительность на полном реальном игровом архиве не измерена. |
| Поддержать первую игру RGO ULJM05752 | Проверяемый профиль `profiles/rgo-uljm05752*.json` | Частично: профиль EBOOT имеет исторические свидетельства; полный текст/шрифт/ISO/gameplay ещё требуют исходных файлов. |
| Поддержать Net Idol Meister | `profiles/nim-unverified.json`, ветвь NIM сценарного codec | Частично: layout проверен на синтетике, профиль явно `verified: false`. Переносить RGO-патчи на вторую игру нельзя. |
| Интегрировать перевод заказчика с корректным чтением обеими играми | Полный workflow, [сценарий приёмки](CUSTOMER_DEMO_RU.md) | Нужны игровые ресурсы, сам перевод заказчика, карта глифов, тестовые сохранения и прохождение затронутых сцен обеих игр. Подготовка перевода с нуля в исходном запросе не поручалась. |

## Лицензирование и браузерная панель

| Требование | Реализация | Статус и границы |
|---|---|---|
| Клиент выдаётся отдельно, команды требуют доступ | `LicensedCommandEntry.cs`, `LicenseSession.cs`, профиль `client-trust.json` | Реализовано. Без издателя — заблокированный preview; без активации рабочие команды недоступны. |
| Тестовые и постоянные ключи, начало при выдаче/активации, лимит устройств | `LicenseAuthority.cs`, `LicenseDurations.cs`, [инструкция владельца](LICENSE_OWNER_RU.md) | Реализовано; длительность, активация, переполнение лимитов и жизненный цикл покрыты licensing self-tests/integration. |
| Продление, пауза, возобновление, отзыв, сброс устройства | `LicenseAuthority.cs`, `ReserveAuthority.cs` | Реализовано. Сброс установки отзывает её старые reserve grants; восстановление доступа требует нового явно выданного допуска. |
| Управление владельцем через React-панель на VPS | `web/license-admin`, `LicenseWebServer.cs`, [развёртывание](ADMIN_WEB_RU.md) | Node 38/38 и native Chromium 32 шага прошли. Работу конкретного VPS/HTTPS подтверждает отдельный отчёт развёртывания. |
| Пароль, TOTP, recovery, безопасные сессии | `WebAdminAuthentication.cs`, `WebPasswordVerification.cs`, `WebSessionControl.cs` | Реализовано: replay checks, PBKDF2, MFA, CSRF, cookie-политика, отзыв сессий. Локальный Chromium-сценарий прошёл; production HTTPS необходимо проверить отдельно. |
| Завершить другие/конкретные сессии владельца | `WebSessionControl.cs`, вкладка сессий | Реализовано с 0.17; позднее завершение проверки пароля не должно оживлять отозванную сессию. |
| Искать лицензии/установки/резерв; страницы и общие счётчики | `OwnerQueries.cs`, [контракт страниц](ADMIN_QUERIES_RU.md) | Реализовано с 0.18: ограниченные страницы, стабильные cursors, смена snapshot при изменении базы, отбор по полям. |
| Медленный вход не блокирует действующую сессию | [конкурентная авторизация](ADMIN_AUTH_CONCURRENCY_RU.md), изолированные лимиты login/MFA, Nginx admission | Реализовано с 0.19; требуется согласованное обновление backend, frontend и proxy. |
| Выдать резервный доступ конкретной установке на 1–168 часов | `ReserveAuthority.cs`, `ReserveCache.cs`, [инструкция](RESERVE_ACCESS_RU.md) | Реализовано. Выдача/первое включение требуют сети, срок не продлевается сам от офлайн-запусков. |
| Отозвать резерв или выключить его глобально | Policy epoch, подписанный grant, локальная фиксация серверного отказа | Реализовано. Отключённый компьютер узнаёт отзыв при связи либо прекращает доступ по подписанному сроку; мгновенный офлайн-отзыв технически не обещается. |
| Не продлевать резерв переводом часов или перезапусками | Монотонный таймер, подписанный checkpoint, фиксируемый отказ | Реализовано. Полный откат VM/ОС и модификация программы владельцем компьютера не предотвращаются аппаратной аттестацией. |
| Защищённый доступ без публикации рабочих секретов | Раздельные backend/admin ports, HTTPS, private state, [модель угроз](LICENSE_SECURITY_RU.md) | Реализовано в коде/шаблонах; секреты создаются при установке. Публичный исходный код допускает изменение клиентской проверки и не является невзламываемым DRM. |

## Проверки этой итерации

На момент заполнения матрицы свежие Core self-tests прошли на штатном .NET 9:
10 групп и 81 независимый CMAC-вектор. Formats self-tests прошли **54/54** после
исправления трёх несовместимых вызовов `StartsWith/EndsWith(char, StringComparison)`.
Синтетические эталоны пересозданы из генератора. Независимые validators подтвердили
ISO (13 записей / 9 файлов / 10 отрицательных случаев), шрифт (66 букв, сохранность
padding/unused bits, PNG) и rebuild ISO (2 замены / 11 сохранённых записей).
CRILAYLA дополнительно проверен на перекрывающихся ссылках, максимальном расстоянии
8194 и потоке размером 256 MiB. Исправлен обход SHA/lineage guard в публичном
`RgoScriptSizeTable.Update`; регрессия воспроизводилась до исправления.

Licensing self-tests прошли **146/146** на штатном .NET 9. Финальный native
Chromium-повтор выполнен с актуальными хэшами исходников и прошёл все 32 шага.
Frontend прошёл **38/38** Node-тестов и offline build. Исправлены несогласованные
пределы длительности выдачи ключей (876000 часов / 36500 дней / 100 лет),
отображение ошибки продления и версия footer. Локальный native Chromium integration
прошёл **32 шага** без diagnostic bridge: активация CLI, выдача/скачивание ключа,
57 лицензий с поиском/страницами, stale cursors, CSRF/anonymous отказ, MFA, сессии,
резерв/отзыв/epoch, restart, recovery replay и мобильный экран; JS-ошибок не было.
Это подтверждает локальный browser transport, **не production HTTPS**.
Полная сборка релизов и работа конкретного VPS подтверждаются их отдельными отчётами.
Адрес, устройство служб и порядок обновления описаны в [руководстве VPS](VPS_DEPLOYMENT_RU.md).

Повторное получение исторического архива и двух EBOOT по первоначальным ссылкам
22.09.2026 вернуло HTTP 404. Поэтому свежая проверка реальных байтов заказчика
не выполнена; приватные URL в публичную документацию не включены.

Аудит всей исходной Git-истории: **11 коммитов, 594 уникальных blob, 359 путей**.
Проверено 7 145 639 байт; шаблоны секретов, известные хэши файлов заказчика,
игровые/шрифтовые бинарники и закрытые файлы издателя не найдены.
Метод и ограничения: [аудит публикации](PUBLICATION_AUDIT_RU.md).

## Что необходимо для окончательной игровой приёмки

1. Для каждой игры получить разрешённые оригинальные `EBOOT.BIN`, `PARAM.SFO`,
   `sc.cpk`, `lt.bin`, чистый ISO и проверить SHA-256/ревизию.
2. Зафиксировать правильную карту глифов и шрифт с разрешением на использование.
3. Сделать no-change round-trip CPK/шрифта, затем перевести короткую и длинную
   реплику, имя, варианты ответа, переносы, Ё/ё и знаки препинания.
4. Повторно извлечь изменённый текст, проверить таблицы размеров/heap/EBOOT и
   собрать отдельный ISO с отчётом сохранности остальных данных.
5. Проверить загрузку, переходы, все затронутые ветки, выборы и save/load в
   зафиксированной версии PPSSPP; отдельно повторить на PSP при требовании её поддержки.
6. Приложить журнал сцен и результаты для каждой игры. Непройденные сцены и
   неподдержанные ревизии перечислять явно; несколько сцен не означают всю игру.
