# C# API and method catalog

This file is generated by `scripts/generate_api_reference.py`. It catalogs every named type, method, constructor, and local function in runtime source. XML comments in the source remain the authoritative detailed documentation.

## `src/LuckyStarPspToolkit.Cli/CommandApplication.cs`

### Types

- `public class CommandApplication` — Represents the toolkit's command application model or service.
- `private class UsageException` — Represents a validated usage exception failure with a stable diagnostic code.
- `private class ParsedArguments` — Represents the toolkit's parsed arguments model or service.

### Methods and functions

- `public int Run(args)` — Parses and executes a command-line invocation and returns a stable process exit code.
- `private int Help(args)` — Executes the <c>help</c> CLI command and returns a stable process exit code.
- `private int Version(args)` — Executes the <c>version</c> CLI command and returns a stable process exit code.
- `private int Inspect(args)` — Inspects the supplied input and returns a structured diagnostic result.
- `private int AuditRgo(args)` — Executes the <c>audit-rgo</c> CLI command and returns a stable process exit code.
- `private int VerifyEboot(args)` — Verifies EBOOT while enforcing the relevant format and safety invariants.
- `private int DecryptEboot(args)` — Executes the <c>decrypt-eboot</c> CLI command and returns a stable process exit code.
- `private int EbootVwfGroups(args)` — Executes the <c>eboot-vwf-groups</c> CLI command and returns a stable process exit code.
- `private int EbootVwfInspect(args)` — Executes the <c>eboot-vwf-inspect</c> CLI command and returns a stable process exit code.
- `private int EbootVwfApply(args)` — Executes the <c>eboot-vwf-apply</c> CLI command and returns a stable process exit code.
- `private int InspectSfo(args)` — Inspects SFO while enforcing the relevant format and safety invariants.
- `private int IsoList(args)` — Executes the <c>iso-list</c> CLI command and returns a stable process exit code.
- `private int IsoExtract(args)` — Executes the <c>iso-extract</c> CLI command and returns a stable process exit code.
- `private int IsoReplace(args)` — Executes the <c>iso-replace</c> CLI command and returns a stable process exit code.
- `private int IsoApplyManifest(args)` — Executes the <c>iso-apply-manifest</c> CLI command and returns a stable process exit code.
- `private int CollectAssets(args)` — Executes the <c>collect-assets</c> CLI command and returns a stable process exit code.
- `private int CpkList(args)` — Executes the <c>cpk-list</c> CLI command and returns a stable process exit code.
- `private int CpkVerify(args)` — Executes the <c>cpk-verify</c> CLI command and returns a stable process exit code.
- `private int CpkExtract(args)` — Executes the <c>cpk-extract</c> CLI command and returns a stable process exit code.
- `private int CpkReplace(args)` — Executes the <c>cpk-replace</c> CLI command and returns a stable process exit code.
- `private int ScriptInspect(args)` — Executes the <c>script-inspect</c> CLI command and returns a stable process exit code.
- `private int GlyphMapValidate(args)` — Executes the <c>glyph-map-validate</c> CLI command and returns a stable process exit code.
- `private int FontInspect(args)` — Executes the <c>font-inspect</c> CLI command and returns a stable process exit code.
- `private int FontImportBdf(args)` — Executes the <c>font-import-bdf</c> CLI command and returns a stable process exit code.
- `private int WorkspaceExport(args)` — Executes the <c>workspace-export</c> CLI command and returns a stable process exit code.
- `private int WorkspaceValidate(args)` — Executes the <c>workspace-validate</c> CLI command and returns a stable process exit code.
- `private int WorkspaceBuild(args)` — Executes the <c>workspace-build</c> CLI command and returns a stable process exit code.
- `private int ApplyEbootPlan(args)` — Applies EBOOT plan while enforcing the relevant format and safety invariants.
- `private int CustomerAudit(args)` — Executes the <c>customer-audit</c> CLI command and returns a stable process exit code.
- `private int FormatsSelfTest(args)` — Formats s self test while enforcing the relevant format and safety invariants.
- `private int SelfTest(args)` — Executes the <c>self-test</c> CLI command and returns a stable process exit code.
- `private byte [] ReadSingleFile(path)` — Reads single file while enforcing the relevant format and safety invariants.
- `private string ExecutableCheckToHuman(check)` — Formats the executable revision, cryptographic checks and patch preconditions for a terminal report.
- `private string WorkspaceValidationToHuman(result)` — Formats scenario counts, changed byte sizes and build warnings without writing game resources.
- `private int ParseIntOption(value, defaultValue, name, minimum, maximum)` — Parses int option while enforcing the relevant format and safety invariants.
- `private long? ParseNullableLongOption(value, name, minimum, maximum)` — Parses nullable long option while enforcing the relevant format and safety invariants.
- `private ushort ParseUShort(value, name)` — Parses u short while enforcing the relevant format and safety invariants.
- `private IReadOnlyCollection < ushort >? ParseIds(value)` — Parses ids while enforcing the relevant format and safety invariants.
- `private bool RangesOverlap(firstStart, firstEndExclusive, secondStart, secondEndExclusive)` — Determines whether two half-open numeric ranges overlap.
- `private IReadOnlyList < string >? ParseVwfGroups(value)` — Parses VWF groups while enforcing the relevant format and safety invariants.
- `private void ProtectReportFromInput(reportPath, inputPath)` — Protects report from input while enforcing the relevant format and safety invariants.
- `private void RequireReportOutside(reportPath, params protectedDirectories)` — Requires report outside while enforcing the relevant format and safety invariants.
- `private void RequireReportDifferent(reportPath, params protectedPaths)` — Requires report different while enforcing the relevant format and safety invariants.
- `private void CommitArtifactsAndWriteReport(artifacts, human, json, jsonPath)` — Commits artifacts and write report while enforcing the relevant format and safety invariants.
- `private void WriteReport(human, json, jsonPath)` — Writes report while enforcing the relevant format and safety invariants.
- `private string Serialize<T>(value)` — Serializes a value to stable UTF-8 JSON for reports or workspaces.
- `private string YesNo(value)` — Formats a Boolean value as stable human-readable yes/no text.
- `private void PrintHelp(writer)` — Writes help while enforcing the relevant format and safety invariants.
- `private constructor ParsedArguments(positionals, options, flags)` — Initializes a new instance with validated constructor state.
- `public string? GetOption(name)` — Gets option while enforcing the relevant format and safety invariants.
- `public bool HasFlag(name)` — Determines whether flag.
- `public ParsedArguments Parse(args, expectedPositionals, valueOptions, flagOptions)` — Parses validated input into the current binary-format model.

## `src/LuckyStarPspToolkit.Core/BinaryPatching.cs`

### Types

- `public class BinaryPatch` — Represents the toolkit's binary patch model or service.
- `public class AppliedBinaryPatch` — Represents the toolkit's applied binary patch model or service.
- `public class BinaryPatchResult` — Represents the toolkit's binary patch result model or service.
- `public class BinaryPatchEngine` — Provides the toolkit's binary patch engine workflow.
- `private record PreparedPatch` — Represents immutable prepared patch data exchanged by the toolkit.
- `public class RgoScriptSizeTable` — Represents the toolkit's RGO script size table model or service.

### Methods and functions

- `public BinaryPatchResult Apply(input, patches, expectedInputSha256)` — Applies the requested transformation after validating all preconditions.
- `private PreparedPatch Prepare(patch)` — Validates a patch name and range and decodes its original and replacement hexadecimal bytes.
- `public byte [] Update(decryptedElf, scriptId, newSizeBytes)` — Updates a script size and all following cumulative table entries in a copy of a verified RGO ELF.

## `src/LuckyStarPspToolkit.Core/Common.cs`

### Types

- `public class ToolkitException` — Controlled exception for malformed, unsupported, or unsafe input.
- `public class Guard` — Represents the toolkit's guard model or service.
- `public class BinaryData` — Represents the toolkit's binary data model or service.
- `public class CryptoUtilities` — Provides the toolkit's crypto utilities workflow.
- `public class HexUtilities` — Provides the toolkit's hex utilities workflow.
- `public class AtomicFile` — Represents the toolkit's atomic file model or service.
- `public class JsonDefaults` — Represents the toolkit's JSON defaults model or service.

### Methods and functions

- `public constructor ToolkitException(message)` — Initializes a new instance with validated constructor state.
- `public constructor ToolkitException(message, innerException)` — Initializes a new instance with validated constructor state.
- `public void Require(condition, message)` — Returns a required value or throws a stable validation error when it is absent.
- `public void RequireRange(dataLength, offset, length, context)` — Requires range while enforcing the relevant format and safety invariants.
- `public ushort ReadUInt16LittleEndian(data, offset, context)` — Reads u int 16 little endian while enforcing the relevant format and safety invariants.
- `public uint ReadUInt32LittleEndian(data, offset, context)` — Reads u int 32 little endian while enforcing the relevant format and safety invariants.
- `public int ReadInt32LittleEndian(data, offset, context)` — Reads int 32 little endian while enforcing the relevant format and safety invariants.
- `public ulong ReadUInt64LittleEndian(data, offset, context)` — Reads u int 64 little endian while enforcing the relevant format and safety invariants.
- `public ushort ReadUInt16BigEndian(data, offset, context)` — Reads u int 16 big endian while enforcing the relevant format and safety invariants.
- `public uint ReadUInt32BigEndian(data, offset, context)` — Reads u int 32 big endian while enforcing the relevant format and safety invariants.
- `public ulong ReadUInt64BigEndian(data, offset, context)` — Reads u int 64 big endian while enforcing the relevant format and safety invariants.
- `public void WriteUInt16LittleEndian(data, offset, value, context)` — Writes u int 16 little endian while enforcing the relevant format and safety invariants.
- `public void WriteUInt32LittleEndian(data, offset, value, context)` — Writes u int 32 little endian while enforcing the relevant format and safety invariants.
- `public ReadOnlySpan < byte > Slice(data, offset, length, context)` — Returns a bounds-checked view over the requested range.
- `public bool StartsWith(data, magic)` — Checks a binary signature without reading past a short input buffer.
- `public bool StartsWithAscii(data, magic)` — Encodes an ASCII signature and compares it to the beginning of the input bytes.
- `public bool IsAllZero(data)` — Determines whether all zero.
- `public string ReadNullTerminatedUtf8(data)` — Reads null terminated UTF 8 while enforcing the relevant format and safety invariants.
- `public string ReadNullTerminatedAscii(data)` — Reads null terminated ASCII while enforcing the relevant format and safety invariants.
- `public int AlignUp(value, alignment)` — Rounds a non-negative value up to the requested positive alignment.
- `public bool ContainsAscii(data, text)` — Determines whether ASCII.
- `public byte [] Sha1(data)` — Computes the 20-byte SHA-1 digest required by the PSP PRX envelope format.
- `public byte [] Sha256(data)` — Computes a 32-byte SHA-256 fingerprint for revision and output integrity checks.
- `public string Sha256Hex(data)` — Computes the lowercase SHA-256 hexadecimal digest of the supplied bytes.
- `public byte [] Aes128CbcEncrypt(plaintext, key)` — Encrypts complete 16-byte blocks with AES-128-CBC, a zero IV and no padding.
- `public byte [] Aes128CbcDecrypt(ciphertext, key)` — Decrypts complete 16-byte blocks with AES-128-CBC, a zero IV and no padding.
- `public byte [] Aes128Cmac(message, key)` — Authenticates a byte sequence with AES-CMAC, including empty and partial final blocks.
- `private byte [] Aes128CbcTransform(input, key, encrypt)` — Validates the AES key and block lengths, then performs the requested zero-IV CBC transform.
- `private byte [] DeriveCmacSubkey(input)` — Derives CMAC subkey while enforcing the relevant format and safety invariants.
- `public string ToHex(data)` — Converts hex while enforcing the relevant format and safety invariants.
- `public string UInt32(value)` — Formats a 32-bit value as a fixed-width uppercase hexadecimal word with a 0x prefix.
- `public string Offset(value)` — Formats a byte offset as an eight-digit uppercase hexadecimal value with a 0x prefix.
- `public byte [] Parse(value)` — Parses validated input into the current binary-format model.
- `public void WriteAllBytes(path, data)` — Writes all bytes while enforcing the relevant format and safety invariants.
- `public void WriteAllText(path, text)` — Writes all text while enforcing the relevant format and safety invariants.
- `private void TryDelete(path)` — Attempts to delete.
- `public JsonSerializerOptions CreateIndented()` — Creates indented while enforcing the relevant format and safety invariants.
- `public string Serialize<T>(value)` — Serializes a value to stable UTF-8 JSON for reports or workspaces.

## `src/LuckyStarPspToolkit.Core/Elf.cs`

### Types

- `public class Elf32Info` — Represents the toolkit's ELF 32 info model or service.
- `public class ElfReader` — Provides ELF reader operations with strict bounds and format validation.

### Methods and functions

- `public bool LooksLike(data)` — Determines whether the supplied data matches the expected format signature.
- `public Elf32Info Parse32LittleEndian(data)` — Parses 32 little endian while enforcing the relevant format and safety invariants.
- `private void ValidateTable(dataLength, offset, entrySize, count, context)` — Validates table while enforcing the relevant format and safety invariants.

## `src/LuckyStarPspToolkit.Core/Inspection.cs`

### Types

- `public enum FileKind` — Defines the supported file kind values.
- `public enum InputContainerKind` — Defines the supported input container kind values.
- `public class FileProbe` — Represents the toolkit's file probe model or service.
- `public class InspectionResult` — Represents the toolkit's inspection result model or service.
- `public class InputSafetyLimits` — Represents the toolkit's input safety limits model or service.
- `public class InputInspector` — Provides the toolkit's input inspector workflow.

### Methods and functions

- `public InspectionResult Inspect(path, limits)` — Inspects the supplied input and returns a structured diagnostic result.
- `public FileProbe Probe(logicalPath, data, compressedSize)` — Classifies one supplied file by its bytes and attempts the corresponding bounded header parser.
- `public string ToHumanText(result)` — Formats the current result as human-readable diagnostic text.
- `private InspectionResult InspectZip(fullPath, limits)` — Inspects ZIP while enforcing the relevant format and safety invariants.
- `private InspectionResult InspectDirectory(root, limits)` — Inspects directory while enforcing the relevant format and safety invariants.
- `private byte [] ReadRegularFile(path, maximumBytes)` — Reads regular file while enforcing the relevant format and safety invariants.
- `private byte [] ReadZipEntryExactly(entry)` — Reads ZIP entry exactly while enforcing the relevant format and safety invariants.
- `private void ReadExactly(stream, destination, context)` — Reads exactly while enforcing the relevant format and safety invariants.
- `private void ValidateEntrySize(name, length, limits)` — Validates entry size while enforcing the relevant format and safety invariants.
- `private void ValidateCompressionRatio(entry, logicalPath, limits)` — Validates compression ratio while enforcing the relevant format and safety invariants.
- `private string NormalizeArchivePath(path)` — Normalizes archive path while enforcing the relevant format and safety invariants.
- `private bool IsDirectoryEntry(entry)` — Determines whether directory entry.
- `private bool HasZipMagic(path)` — Determines whether ZIP magic.
- `private bool LooksLikeIso9660(data)` — Checks the primary ISO descriptor signature at logical sector 16; this is not full ISO validation.
- `private string DetermineRole(probe)` — Determines role while enforcing the relevant format and safety invariants.
- `private IReadOnlyList < string > BuildWarnings(probes)` — Builds warnings while enforcing the relevant format and safety invariants.
- `private void ValidateLimits(limits)` — Validates limits while enforcing the relevant format and safety invariants.
- `private void TryParse(action, ref error)` — Attempts to parse.
- `private void AppendOptional(output, label, value)` — Appends optional while enforcing the relevant format and safety invariants.
- `private string YesNo(value)` — Formats a Boolean value as stable human-readable yes/no text.
- `private string EnumText<T>(value)` — Converts an enum name into the kebab-case spelling used in inspection reports.

## `src/LuckyStarPspToolkit.Core/PspPrx.cs`

### Types

- `public class PspModuleHeader` — Represents the toolkit's PSP module header model or service.
- `public class PspDecryptionResult` — Represents the toolkit's PSP decryption result model or service.
- `public class PspPrxReader` — Provides PSP PRX reader operations with strict bounds and format validation.

### Methods and functions

- `public bool LooksLike(data)` — Determines whether the supplied data matches the expected format signature.
- `public bool CanDecrypt(header)` — Checks whether the PRX tag is supported; authentication still occurs during decryption.
- `public PspModuleHeader ParseHeader(data)` — Parses header while enforcing the relevant format and safety invariants.
- `public PspDecryptionResult DecryptVerified(input)` — Authenticates a supported PSP PRX and returns its verified decrypted ELF payload.
- `private byte [] ExpandSeed()` — Expands seed while enforcing the relevant format and safety invariants.
- `private void CopyField(source, sourceOffset, destination, destinationOffset, length, context)` — Copies field while enforcing the relevant format and safety invariants.
- `private void XorInPlace(value, mask)` — XORs equal-length byte spans for PRX key derivation, rejecting inconsistent lengths.
- `private string ReadModuleName(data)` — Reads module name while enforcing the relevant format and safety invariants.
- `private int ToInt32(value, context)` — Converts int 32 while enforcing the relevant format and safety invariants.

## `src/LuckyStarPspToolkit.Core/RgoProfile.cs`

### Types

- `public enum ExecutableCompatibility` — Defines the supported executable compatibility values.
- `public class PatchPointCheck` — Represents the toolkit's patch point check model or service.
- `public class RgoScriptSlot` — Represents the toolkit's RGO script slot model or service.
- `public class RgoExecutableCheck` — Represents the toolkit's RGO executable check model or service.
- `public class RgoResources` — Represents the toolkit's RGO resources model or service.
- `public class RgoAudit` — Represents the toolkit's RGO audit model or service.
- `public class RgoProfile` — Provides the toolkit's RGO profile workflow.

### Methods and functions

- `public RgoExecutableCheck VerifyExecutable(source)` — Verifies executable while enforcing the relevant format and safety invariants.
- `public RgoAudit Audit(inspection)` — Audits the supplied input and returns a structured safety and compatibility report.
- `public string AuditToHumanText(audit)` — Audits to human text while enforcing the relevant format and safety invariants.
- `public byte [] DecryptForPatching(source)` — Decrypts for patching while enforcing the relevant format and safety invariants.
- `private IReadOnlyList < RgoScriptSlot > ReadScriptSlots(plaintext, out consistent)` — Reads script slots while enforcing the relevant format and safety invariants.
- `private IReadOnlyList < PatchPointCheck > VerifyPatchPoints(plaintext)` — Verifies patch points while enforcing the relevant format and safety invariants.
- `private RgoExecutableCheck FailureResult(encrypted, encryptedHash, exactEncrypted, pspHeader, failure)` — Builds a failed executable-check result while retaining available source and PRX header evidence.
- `private string YesNo(value)` — Formats a Boolean value as stable human-readable yes/no text.

## `src/LuckyStarPspToolkit.Core/RgoVwfPatchProfile.cs`

### Types

- `public enum RgoEbootPatchWordState` — Defines the supported RGO EBOOT patch word state values.
- `public record RgoEbootPatchDefinition` — Represents immutable RGO EBOOT patch definition data exchanged by the toolkit.
- `public class RgoEbootPatchWordStatus` — Represents the toolkit's RGO EBOOT patch word status model or service.
- `public record RgoEbootPatchGroupInfo` — Represents immutable RGO EBOOT patch group info data exchanged by the toolkit.
- `public class RgoVwfPatchInspection` — Represents the toolkit's RGO VWF patch inspection model or service.
- `public class RgoVwfPatchResult` — Represents the toolkit's RGO VWF patch result model or service.
- `public class RgoVwfPatchProfile` — Provides the toolkit's RGO VWF patch profile workflow.
- `private record PreparedExecutable` — Represents immutable prepared executable data exchanged by the toolkit.
- `private record ScriptHeapState` — Represents immutable script heap state data exchanged by the toolkit.

### Methods and functions

- `public IReadOnlyList < string > ResolveGroups(groups)` — Resolves groups while enforcing the relevant format and safety invariants.
- `public RgoVwfPatchInspection Inspect(source, groups)` — Inspects the supplied input and returns a structured diagnostic result.
- `public RgoVwfPatchResult Apply(source, groups)` — Applies the requested transformation after validating all preconditions.
- `private string BuildInspectionMessage(recognizedWords, scriptTableConsistent, scriptHeap, scriptHeapConsistent, requiredScriptHeapBytes, compatibleLineage, alreadyApplied, patchedCount)` — Builds inspection message while enforcing the relevant format and safety invariants.
- `private bool IsScriptSizeTableConsistent(source)` — Determines whether script size table consistent.
- `private int GetRequiredScriptHeapBytes(source)` — Gets required script heap bytes while enforcing the relevant format and safety invariants.
- `private ScriptHeapState ReadScriptHeap(source)` — Reads script heap while enforcing the relevant format and safety invariants.
- `private void VerifySelectedWords(output, selected)` — Verifies selected words while enforcing the relevant format and safety invariants.
- `private PreparedExecutable PrepareExecutable(source)` — Prepares executable while enforcing the relevant format and safety invariants.
- `private string ToLittleEndianHex(value)` — Converts little endian hex while enforcing the relevant format and safety invariants.

## `src/LuckyStarPspToolkit.Core/SelfDiagnostics.cs`

### Types

- `public class DiagnosticCaseResult` — Represents the toolkit's diagnostic case result model or service.
- `public class RuntimeSelfTestReport` — Represents the toolkit's runtime self test report model or service.
- `public class SelfDiagnostics` — Provides the toolkit's self diagnostics workflow.

### Methods and functions

- `private string ResolveToolkitVersion()` — Resolves toolkit version while enforcing the relevant format and safety invariants.
- `public RuntimeSelfTestReport Run()` — Runs the built-in diagnostic cases and returns their individual results and aggregate status.
- `public string ToHumanText(report)` — Formats the current result as human-readable diagnostic text.
- `private void TestSha256()` — Verifies SHA 256 behavior and invariants.
- `private void TestAesCbc()` — Verifies aes cbc behavior and invariants.
- `private void TestCmacEmpty()` — Verifies CMAC empty behavior and invariants.
- `private void TestCmacBlock()` — Verifies CMAC block behavior and invariants.
- `private void TestSfo()` — Verifies SFO behavior and invariants.
- `private void TestElf()` — Verifies ELF behavior and invariants.
- `private void TestRgoVwfProfile()` — Verifies RGO VWF profile behavior and invariants.
- `private bool RangesOverlap(firstStart, firstEndExclusive, secondStart, secondEndExclusive)` — Determines whether two half-open numeric ranges overlap.
- `private void TestRangeFailure()` — Verifies range failure behavior and invariants.
- `private byte [] CreateSyntheticSfo()` — Creates synthetic SFO while enforcing the relevant format and safety invariants.
- `private void WriteIndex(data, offset, keyOffset, format, length, maximumLength, dataOffset)` — Writes index while enforcing the relevant format and safety invariants.
- `private void RunCase(results, name, test)` — Runs one deterministic self-test case and records its result.
- `private void RequireEqual(actual, expected, context)` — Requires equal while enforcing the relevant format and safety invariants.

## `src/LuckyStarPspToolkit.Core/Sfo.cs`

### Types

- `public enum SfoValueKind` — Defines the supported SFO value kind values.
- `public class SfoEntry` — Represents the toolkit's SFO entry model or service.
- `public class SfoFile` — Represents the toolkit's SFO file model or service.
- `public class SfoReader` — Provides SFO reader operations with strict bounds and format validation.

### Methods and functions

- `public constructor SfoFile(version, entries)` — Initializes a new instance with validated constructor state.
- `public SfoEntry ? Find(key)` — Looks up an exact PARAM.SFO key and returns null when the key is absent.
- `public string? GetString(key)` — Gets string while enforcing the relevant format and safety invariants.
- `public uint? GetUInt32(key)` — Gets u int 32 while enforcing the relevant format and safety invariants.
- `public bool LooksLike(data)` — Determines whether the supplied data matches the expected format signature.
- `public SfoFile Parse(data)` — Parses validated input into the current binary-format model.
- `private SfoEntry ParseEntry(key, format, length, maximumLength, raw)` — Parses entry while enforcing the relevant format and safety invariants.
- `private string ReadKey(data, offset, limit)` — Reads key while enforcing the relevant format and safety invariants.
- `private int ToInt32(value, context)` — Converts int 32 while enforcing the relevant format and safety invariants.

## `src/LuckyStarPspToolkit.Formats/Audit/CustomerArchiveAuditor.cs`

### Types

- `public class CustomerAuditReport` — Represents the toolkit's customer audit report model or service.
- `public class CustomerAuditFile` — Represents the toolkit's customer audit file model or service.
- `public class CustomerArchiveAuditor` — Provides the toolkit's customer archive auditor workflow.

### Methods and functions

- `public CustomerAuditReport Audit(source, limits)` — Audits the supplied input and returns a structured safety and compatibility report.
- `public void Save(report, path)` — Persists the validated data to its destination.
- `private void AuditDirectory(directory, report, limits)` — Audits directory while enforcing the relevant format and safety invariants.
- `private void AuditZip(path, report, limits)` — Audits ZIP while enforcing the relevant format and safety invariants.
- `private CustomerAuditFile AuditStream(path, stream, size, compressedSize, limits)` — Audits stream while enforcing the relevant format and safety invariants.
- `private string DetectKind(path, prefix, allZero)` — Detects kind while enforcing the relevant format and safety invariants.
- `private string NormalizeZipPath(value)` — Normalizes ZIP path while enforcing the relevant format and safety invariants.
- `private void FinalizeReport(report)` — Finalizes report while enforcing the relevant format and safety invariants.

## `src/LuckyStarPspToolkit.Formats/Common/AtomicFile.cs`

### Types

- `public record AtomicWriteRequest` — Represents immutable atomic write request data exchanged by the toolkit.
- `public class AtomicFile` — Represents the toolkit's atomic file model or service.
- `private class StagedWrite` — Represents the toolkit's staged write model or service.

### Methods and functions

- `public void WriteAllBytes(path, data)` — Writes all bytes while enforcing the relevant format and safety invariants.
- `public void WriteAllText(path, value)` — Writes all text while enforcing the relevant format and safety invariants.
- `public void WriteStream(path, writer)` — Writes a file through a staged stream and atomically replaces the destination after a durable flush.
- `public void WriteAll(params requests)` — Commits a set of file writes as one best-effort transactional operation with rollback.
- `private void WriteStagingFile(path, data)` — Writes staging file while enforcing the relevant format and safety invariants.
- `private void TryDelete(path)` — Attempts to delete.

## `src/LuckyStarPspToolkit.Formats/Common/BinaryBufferWriter.cs`

### Types

- `internal class BinaryBufferWriter` — Provides binary buffer writer operations with strict bounds and format validation.

### Methods and functions

- `public constructor BinaryBufferWriter(length)` — Initializes a new instance with validated constructor state.
- `public void WriteByte(value)` — Writes byte while enforcing the relevant format and safety invariants.
- `public void WriteUInt16LittleEndian(value)` — Writes u int 16 little endian while enforcing the relevant format and safety invariants.
- `public void WriteUInt16BigEndian(value)` — Writes u int 16 big endian while enforcing the relevant format and safety invariants.
- `public void WriteUInt32LittleEndian(value)` — Writes u int 32 little endian while enforcing the relevant format and safety invariants.
- `public void WriteUInt32BigEndian(value)` — Writes u int 32 big endian while enforcing the relevant format and safety invariants.
- `public void WriteUInt64LittleEndian(value)` — Writes u int 64 little endian while enforcing the relevant format and safety invariants.
- `public void WriteUInt64BigEndian(value)` — Writes u int 64 big endian while enforcing the relevant format and safety invariants.
- `public void WriteBytes(data)` — Writes bytes while enforcing the relevant format and safety invariants.
- `public void WriteBytesAt(offset, data)` — Writes bytes at while enforcing the relevant format and safety invariants.
- `private void Ensure(offset, length)` — Checks a required invariant and throws a stable validation error when it fails.

## `src/LuckyStarPspToolkit.Formats/Common/BinarySpanReader.cs`

### Types

- `internal struct BinarySpanReader` — Provides binary span reader operations with strict bounds and format validation.

### Methods and functions

- `public constructor BinarySpanReader(data, position)` — Initializes a new instance with validated constructor state.
- `public byte ReadByte()` — Reads byte while enforcing the relevant format and safety invariants.
- `public ushort ReadUInt16LittleEndian()` — Reads u int 16 little endian while enforcing the relevant format and safety invariants.
- `public ushort ReadUInt16BigEndian()` — Reads u int 16 big endian while enforcing the relevant format and safety invariants.
- `public uint ReadUInt32LittleEndian()` — Reads u int 32 little endian while enforcing the relevant format and safety invariants.
- `public uint ReadUInt32BigEndian()` — Reads u int 32 big endian while enforcing the relevant format and safety invariants.
- `public ulong ReadUInt64LittleEndian()` — Reads u int 64 little endian while enforcing the relevant format and safety invariants.
- `public ulong ReadUInt64BigEndian()` — Reads u int 64 big endian while enforcing the relevant format and safety invariants.
- `public ReadOnlySpan < byte > ReadBytes(length)` — Reads bytes while enforcing the relevant format and safety invariants.
- `public ReadOnlySpan < byte > Slice(offset, length)` — Returns a bounds-checked view over the requested range.
- `public string ReadNullTerminatedUtf8At(offset, maximumBytes)` — Reads null terminated UTF 8 at while enforcing the relevant format and safety invariants.
- `private void Ensure(length)` — Checks a required invariant and throws a stable validation error when it fails.

## `src/LuckyStarPspToolkit.Formats/Common/BinaryUtilities.cs`

### Types

- `public class BinaryUtilities` — Provides the toolkit's binary utilities workflow.

### Methods and functions

- `public int Align(value, alignment)` — Rounds a non-negative value up to the requested positive alignment.
- `public long Align(value, alignment)` — Rounds a non-negative value up to the requested positive alignment.
- `public string Sha256Hex(data)` — Computes the lowercase SHA-256 hexadecimal digest of the supplied bytes.
- `public string Sha256HexFile(path)` — Streams a protected file through SHA-256 and rejects a length change during hashing.
- `public byte [] ParseHex(value)` — Parses hex while enforcing the relevant format and safety invariants.
- `public byte [] ReadAllBytesBounded(path, limits)` — Reads a regular file once while enforcing configured size and reparse-point protections.
- `public byte [] Utf8(value)` — Encodes text as strict UTF-8 without a byte-order mark.

## `src/LuckyStarPspToolkit.Formats/Common/FileLimits.cs`

### Types

- `public record FileLimits` — Represents immutable file limits data exchanged by the toolkit.

## `src/LuckyStarPspToolkit.Formats/Common/Guard.cs`

### Types

- `internal class Guard` — Represents the toolkit's guard model or service.

### Methods and functions

- `public void Range(condition, code, message)` — Validates a nonnegative offset and length using subtraction to avoid overflow in a bounds check.
- `public int CheckedInt(value, code, name)` — Checks whether a numeric value fits an Int32 and reports a caller-specific validation error otherwise.
- `public ushort CheckedUInt16(value, code, name)` — Checks whether a numeric value fits a UInt16 and reports a caller-specific validation error otherwise.
- `public uint CheckedUInt32(value, code, name)` — Checks whether a numeric value fits a UInt32 and reports a caller-specific validation error otherwise.

## `src/LuckyStarPspToolkit.Formats/Common/PathUtilities.cs`

### Types

- `public class PathUtilities` — Provides the toolkit's path utilities workflow.

### Methods and functions

- `public string Normalize(path)` — Returns an absolute filesystem path without a trailing separator, except for a filesystem root.
- `public string NormalizeProtectedPath(path, code)` — Normalizes a path and checks every existing ancestor from its filesystem root before protected I/O.
- `public bool AreSame(left, right)` — Compares normalized path spellings using the host platform case rules; it does not compare hard-link identities.
- `public bool IsWithinOrSame(candidate, root)` — Checks normalized path equality or containment with a separator boundary, including filesystem roots.
- `public void RequireDifferent(left, right, code, message)` — Rejects an output path whose normalized spelling equals a protected input path.
- `public string ResolveContainedPath(root, relative, code)` — Resolves a portable relative manifest path under its root, rejecting traversal, drive syntax and existing links.
- `private void RejectExistingReparsePoints(root, candidate, code)` — Checks each existing ancestor between the root and candidate for symbolic links or reparse points.
- `private void CheckReparsePoint(path, code)` — Queries an existing directory entry directly and rejects symbolic links or Windows reparse points.

## `src/LuckyStarPspToolkit.Formats/Common/StrictJson.cs`

### Types

- `public class StrictJson` — Reads operation manifests without ambiguous duplicate JSON property names.

### Methods and functions

- `public T Deserialize<T>(data, options, errorCode)` — Validates every object scope before deserializing the same bounded UTF-8 snapshot.

## `src/LuckyStarPspToolkit.Formats/Common/ToolkitBuildInfo.cs`

### Types

- `public class ToolkitBuildInfo` — Represents the toolkit's toolkit build info model or service.

### Methods and functions

- `private string ResolveVersion()` — Resolves version while enforcing the relevant format and safety invariants.

## `src/LuckyStarPspToolkit.Formats/Common/ToolkitException.cs`

### Types

- `public class ToolkitException` — Represents a validated toolkit exception failure with a stable diagnostic code.

### Methods and functions

- `public constructor ToolkitException(code, message)` — Initializes a new instance with validated constructor state.
- `public constructor ToolkitException(code, message, innerException)` — Initializes a new instance with validated constructor state.
- `public string ToString()` — Returns the stable diagnostic string representation.

## `src/LuckyStarPspToolkit.Formats/Cri/CriCpkArchive.cs`

### Types

- `public class CriCpkEntry` — Represents the toolkit's CRI CPK entry model or service.
- `public record CriCpkBuildResult` — Represents immutable CRI CPK build result data exchanged by the toolkit.
- `public class CriCpkArchive` — Represents the toolkit's CRI CPK archive model or service.
- `private record EntryDescriptor` — Represents immutable entry descriptor data exchanged by the toolkit.

### Methods and functions

- `public byte [] GetExtractedData(limits)` — Gets extracted data while enforcing the relevant format and safety invariants.
- `public CriCpkEntry Clone()` — Creates a deep copy whose mutable buffers are independent of the source instance.
- `private constructor CriCpkArchive(originalBytes, headerPacket, itocPacket, dataL, dataH, entries, originalLowRows, originalHighRows, itocSizeBias)` — Initializes a new instance with validated constructor state.
- `public CriCpkArchive Parse(data, limits)` — Parses validated input into the current binary-format model.
- `public CriCpkEntry GetEntry(id)` — Gets entry while enforcing the relevant format and safety invariants.
- `public void ReplaceEntry(id, uncompressedData)` — Replaces entry while enforcing the relevant format and safety invariants.
- `public CriCpkBuildResult Build(limits)` — Serializes the current validated model into its binary representation.
- `private Dictionary < ushort , CriUtfRow > ReadDescriptors(table, low, output)` — Reads descriptors while enforcing the relevant format and safety invariants.
- `private CriUtfTable BuildDataTable(template, entries, originalRows)` — Builds data table while enforcing the relevant format and safety invariants.
- `private void SetIfPresent(table, column, value)` — Sets if present while enforcing the relevant format and safety invariants.
- `private void ValidateContiguousIds(ids)` — Validates contiguous ids while enforcing the relevant format and safety invariants.

## `src/LuckyStarPspToolkit.Formats/Cri/CriUtfCodec.cs`

### Types

- `public class CriUtfCodec` — Provides CRI UTF codec operations with strict bounds and format validation.
- `private class StringPool` — Represents the toolkit's string pool model or service.
- `private class DataPool` — Represents the toolkit's data pool model or service.

### Methods and functions

- `public CriUtfPacket ParsePacket(packetBytes, limits)` — Parses packet while enforcing the relevant format and safety invariants.
- `public byte [] BuildPacket(packet, encrypt)` — Builds packet while enforcing the relevant format and safety invariants.
- `public CriUtfTable ParseTable(input, limits)` — Parses table while enforcing the relevant format and safety invariants.
- `public byte [] BuildTable(table)` — Builds table while enforcing the relevant format and safety invariants.
- `public void TransformPacket(data)` — Transforms packet while enforcing the relevant format and safety invariants.
- `private CriUtfType ParseType(nibble, columnIndex)` — Parses type while enforcing the relevant format and safety invariants.
- `private CriUtfValue ReadValue(ref reader, table, stringsOffset, dataOffset, type, context)` — Reads value while enforcing the relevant format and safety invariants.
- `private CriUtfValue ReadData(ref reader, table, dataOffset, context)` — Reads data while enforcing the relevant format and safety invariants.
- `private string ReadString(table, stringsOffset, dataOffset, relativeOffset, context)` — Reads string while enforcing the relevant format and safety invariants.
- `private void WriteValue(writer, value, type, stringOffset, dataOffset)` — Writes value while enforcing the relevant format and safety invariants.
- `private int ValueSize(type)` — Returns the encoded byte width of one supported CRI UTF field type.
- `private void ValidateValueForType(value, type, context)` — Validates value for type while enforcing the relevant format and safety invariants.
- `private void ValidateOffset(offset, length, name)` — Validates offset while enforcing the relevant format and safety invariants.
- `public uint Add(value)` — Interns a UTF table string or data blob and returns its offset in the corresponding pool.
- `public byte [] ToArray()` — Copies the current validated sequence into a new array.
- `public uint Add(data)` — Interns a UTF table string or data blob and returns its offset in the corresponding pool.
- `public byte [] ToArray()` — Copies the current validated sequence into a new array.

## `src/LuckyStarPspToolkit.Formats/Cri/CriUtfModels.cs`

### Types

- `public enum CriUtfStorage` — Defines the supported CRI UTF storage values.
- `public enum CriUtfType` — Defines the supported CRI UTF type values.
- `public class CriUtfValue` — Represents the toolkit's CRI UTF value model or service.
- `public class CriUtfColumn` — Represents the toolkit's CRI UTF column model or service.
- `public class CriUtfRow` — Represents the toolkit's CRI UTF row model or service.
- `public class CriUtfTable` — Represents the toolkit's CRI UTF table model or service.
- `public record CriUtfPacket` — Represents immutable CRI UTF packet data exchanged by the toolkit.

### Methods and functions

- `public CriUtfValue Clone()` — Creates a deep copy whose mutable buffers are independent of the source instance.
- `public CriUtfValue FromUnsigned(value)` — Creates unsigned while enforcing the relevant format and safety invariants.
- `public CriUtfValue FromSingle(value)` — Creates single while enforcing the relevant format and safety invariants.
- `public CriUtfValue FromText(value)` — Creates text while enforcing the relevant format and safety invariants.
- `public CriUtfValue FromData(value)` — Creates data while enforcing the relevant format and safety invariants.
- `public CriUtfColumn Clone()` — Creates a deep copy whose mutable buffers are independent of the source instance.
- `public CriUtfRow Clone()` — Creates a deep copy whose mutable buffers are independent of the source instance.
- `public int GetColumnIndex(name)` — Gets column index while enforcing the relevant format and safety invariants.
- `public bool TryGetColumnIndex(name, out index)` — Attempts to get column index.
- `public ulong GetUnsigned(rowIndex, columnName)` — Gets unsigned while enforcing the relevant format and safety invariants.
- `public string GetText(rowIndex, columnName)` — Gets text while enforcing the relevant format and safety invariants.
- `public byte [] GetData(rowIndex, columnName)` — Gets data while enforcing the relevant format and safety invariants.
- `public void SetUnsigned(rowIndex, columnName, value)` — Sets unsigned while enforcing the relevant format and safety invariants.
- `public void SetData(rowIndex, columnName, value)` — Sets data while enforcing the relevant format and safety invariants.
- `public void SetText(rowIndex, columnName, value)` — Sets text while enforcing the relevant format and safety invariants.
- `public CriUtfTable Clone()` — Creates a deep copy whose mutable buffers are independent of the source instance.
- `public CriUtfRow CreateEmptyRow()` — Creates empty row while enforcing the relevant format and safety invariants.
- `private void ValidateRow(rowIndex)` — Validates row while enforcing the relevant format and safety invariants.
- `private void PromoteIfNecessary(column, value)` — Promotes a constant or zero UTF column to per-row storage before writing a different row value.
- `private void EnsureNumeric(type, name)` — Ensures numeric while enforcing the relevant format and safety invariants.
- `private void ValidateUnsignedFits(type, value, name)` — Validates unsigned fits while enforcing the relevant format and safety invariants.

## `src/LuckyStarPspToolkit.Formats/Cri/CrilaylaCodec.cs`

### Types

- `public record CrilaylaInfo` — Represents immutable crilayla info data exchanged by the toolkit.
- `public class CrilaylaCodec` — Provides crilayla codec operations with strict bounds and format validation.
- `private struct ReverseBitReader` — Provides reverse bit reader operations with strict bounds and format validation.

### Methods and functions

- `public bool IsFrame(data)` — Determines whether frame.
- `public CrilaylaInfo Inspect(data, limits)` — Inspects the supplied input and returns a structured diagnostic result.
- `public byte [] Decompress(data, limits)` — Decodes a bounded reverse-bitstream CRILAYLA frame, validating every output and back-reference range.
- `private int CheckedSize(value, name)` — Converts a CRILAYLA size field to an allocation-safe integer under the configured limit.
- `public constructor ReverseBitReader(data)` — Initializes a new instance with validated constructor state.
- `public uint ReadBits(count)` — Reads bits while enforcing the relevant format and safety invariants.

## `src/LuckyStarPspToolkit.Formats/Diagnostics/FormatSelfDiagnostics.cs`

### Types

- `public record FormatDiagnosticCaseResult` — Represents immutable format diagnostic case result data exchanged by the toolkit.
- `public record FormatSelfTestReport` — Represents immutable format self test report data exchanged by the toolkit.
- `public class FormatSelfDiagnostics` — Provides the toolkit's format self diagnostics workflow.

### Methods and functions

- `public FormatSelfTestReport Run()` — Runs the built-in diagnostic cases and returns their individual results and aggregate status.
- `public string ToHumanText(report)` — Formats the current result as human-readable diagnostic text.
- `private void TestGlyphMap()` — Verifies glyph map behavior and invariants.
- `private void TestChecksum()` — Verifies checksum behavior and invariants.
- `private void TestLtFont()` — Verifies lt font behavior and invariants.
- `private void TestUtfRoundTrip()` — Verifies UTF round trip behavior and invariants.
- `private void RunCase(output, name, test)` — Runs one deterministic self-test case and records its result.

## `src/LuckyStarPspToolkit.Formats/Fonts/BdfFont.cs`

### Types

- `public record BdfBoundingBox` — Represents immutable BDF bounding box data exchanged by the toolkit.
- `public class BdfGlyph` — Represents the toolkit's BDF glyph model or service.
- `public record BdfRasterizedGlyph` — Represents immutable BDF rasterized glyph data exchanged by the toolkit.
- `public class BdfFont` — Represents the toolkit's BDF font model or service.

### Methods and functions

- `private constructor BdfFont(glyphs, ascent, descent, declaredGlyphCount, sourceSha256)` — Initializes a new instance with validated constructor state.
- `public BdfFont Load(path, limits)` — Loads and validates the requested data from its source.
- `public bool TryGetGlyph(rune, out glyph)` — Attempts to get glyph.
- `public BdfRasterizedGlyph Rasterize(rune, targetWidth, targetHeight, baseline, intensity, xShift, yShift)` — Places a BDF bitmap in the fixed LT cell and reports clipping rather than silently truncating a glyph.
- `private BdfFont Parse(lines, sourceSha256, limits)` — Parses validated input into the current binary-format model.
- `private void ValidateBox(box, line)` — Validates box while enforcing the relevant format and safety invariants.
- `private string [] SplitValues(line)` — Tokenizes a BDF directive on whitespace for strict numeric field parsing.
- `private int ParseSingleInteger(line, name)` — Parses single integer while enforcing the relevant format and safety invariants.
- `private int ParseInteger(value, name, line)` — Parses integer while enforcing the relevant format and safety invariants.
- `private byte [] ReadTextFile(path, maximumBytes)` — Reads text file while enforcing the relevant format and safety invariants.

## `src/LuckyStarPspToolkit.Formats/Fonts/LtFont.cs`

### Types

- `public record GlyphInkBounds` — Represents immutable glyph ink bounds data exchanged by the toolkit.
- `public class LtGlyph` — Represents the toolkit's lt glyph model or service.
- `public record LtFontRussianGlyphStatus` — Represents immutable lt font russian glyph status data exchanged by the toolkit.
- `public record LtFontAnalysis` — Represents immutable lt font analysis data exchanged by the toolkit.
- `public class LtFont` — Represents the toolkit's lt font model or service.

### Methods and functions

- `public GlyphInkBounds ? GetInkBounds()` — Gets ink bounds while enforcing the relevant format and safety invariants.
- `public LtGlyph Clone()` — Creates a deep copy whose mutable buffers are independent of the source instance.
- `private constructor LtFont(glyphs, padding, fileSize, sourceSha256)` — Initializes a new instance with validated constructor state.
- `public LtFont Parse(data, glyphCount)` — Parses validated input into the current binary-format model.
- `public LtFont Clone()` — Creates a deep copy whose mutable buffers are independent of the source instance.
- `public LtGlyph GetGlyph(index)` — Gets glyph while enforcing the relevant format and safety invariants.
- `public void ReplaceGlyph(index, levels, advanceWidth, reserved)` — Replaces glyph while enforcing the relevant format and safety invariants.
- `public byte [] Build()` — Serializes the current validated model into its binary representation.
- `public LtFontAnalysis Analyze(map)` — Analyzes the supplied model and returns deterministic diagnostics.
- `public RgbaImage RenderAtlas(columns, scale, gutter, limits)` — Renders decoded LT glyphs into a bounded RGBA atlas using the requested columns, scale and gutter.

## `src/LuckyStarPspToolkit.Formats/Fonts/LtFontPatcher.cs`

### Types

- `public enum LtFontPatchSelection` — Defines the supported lt font patch selection values.
- `public record LtFontPatchOptions` — Represents immutable lt font patch options data exchanged by the toolkit.
- `public record LtFontPatchItem` — Represents immutable lt font patch item data exchanged by the toolkit.
- `public record LtFontPatchResult` — Represents immutable lt font patch result data exchanged by the toolkit.
- `public class LtFontPatcher` — Provides lt font patcher operations with strict bounds and format validation.

### Methods and functions

- `public LtFontPatchResult ApplyBdf(source, map, bdf, options)` — Rasterizes selected BDF glyphs into a cloned LT font while preserving untouched binary metadata.
- `public LtFontPatchSelection ParseSelection(value)` — Parses selection while enforcing the relevant format and safety invariants.
- `private bool TrySingleRune(value, out rune)` — Attempts to single rune.
- `private bool IsSelected(rune, selection)` — Determines whether selected.

## `src/LuckyStarPspToolkit.Formats/Fonts/PngWriter.cs`

### Types

- `public class RgbaImage` — Represents the toolkit's rgba image model or service.
- `public class PngWriter` — Provides PNG writer operations with strict bounds and format validation.

### Methods and functions

- `public constructor RgbaImage(width, height, pixels)` — Initializes a new instance with validated constructor state.
- `public byte [] Encode(image, limits)` — Encodes validated text or binary state into the target representation.
- `private void WriteChunk(output, type, data)` — Writes chunk while enforcing the relevant format and safety invariants.
- `private uint UpdateCrc(crc, data)` — Updates CRC while enforcing the relevant format and safety invariants.
- `private uint [] BuildCrcTable()` — Builds CRC table while enforcing the relevant format and safety invariants.

## `src/LuckyStarPspToolkit.Formats/Iso/Iso9660Image.cs`

### Types

- `public class Iso9660Image` — Represents the toolkit's ISO 9660 image model or service.
- `private record ParsedDirectoryRecord` — Represents immutable parsed directory record data exchanged by the toolkit.
- `private class PendingEntry` — Represents the toolkit's pending entry model or service.

### Methods and functions

- `private constructor Iso9660Image(source, limits, sourceName, volumeIdentifier, volumeBlockCount, logicalBlockSize, primaryVolumeDescriptorOffset, hasSupplementaryVolumeDescriptor, hasPartitionDescriptor, hasUnsupportedVolumeDescriptor, root, byPath)` — Initializes a new instance with validated constructor state.
- `public Iso9660Image Open(path, limits)` — Opens a protected ISO path and validates its descriptors and bounded directory tree.
- `internal Iso9660Image ParseStream(stream, sourceName, limits, leaveOpen)` — Parses stream while enforcing the relevant format and safety invariants.
- `public Iso9660Image Parse(data, limits)` — Parses validated input into the current binary-format model.
- `public Iso9660Summary CreateSummary()` — Creates summary while enforcing the relevant format and safety invariants.
- `public long EstimateListingBytes()` — Estimates a conservative UTF-8 JSON listing size before allocating a complete report.
- `public bool TryGetEntry(path, out entry)` — Attempts to get entry.
- `public Iso9660Entry GetEntry(path)` — Gets entry while enforcing the relevant format and safety invariants.
- `public byte [] ReadFile(path, maximumBytes)` — Reads file while enforcing the relevant format and safety invariants.
- `public void ExtractFile(path, outputPath)` — Extracts file while enforcing the relevant format and safety invariants.
- `public void CopyFileTo(path, destination, hash)` — Copies file to while enforcing the relevant format and safety invariants.
- `public void CopyFileTo(entry, destination, hash)` — Copies file to while enforcing the relevant format and safety invariants.
- `public string ComputeFileSha256(path)` — Computes SHA-256 over exactly one ISO file payload, including all of its extents in order.
- `public string ComputeSourceSha256()` — Computes SHA-256 over the complete source image.
- `internal void CopySourceTo(destination, hash)` — Copies source to while enforcing the relevant format and safety invariants.
- `internal void ReadSourceExactly(offset, destination, context)` — Reads source exactly while enforcing the relevant format and safety invariants.
- `public void Dispose()` — Releases owned streams and other disposable resources.
- `private Iso9660Image ParseCore(source, limits, sourceName)` — Parses core while enforcing the relevant format and safety invariants.
- `private void ParseDirectoryRecursive(source, directory, parentPath, blockSize, volumeSize, limits, byPath, visitedDirectories, ref extentCount, ref totalDirectoryBytes, depth)` — Parses directory recursive while enforcing the relevant format and safety invariants.
- `private Iso9660DirectoryRecordLocation CreateRecordLocation(record, physicalOffset, recordLength)` — Creates record location while enforcing the relevant format and safety invariants.
- `private long MapDirectoryDataOffsetToSource(directory, blockSize, logicalOffset, byteLength)` — Maps a directory-relative record range into one physical source extent, rejecting cross-extent records.
- `private void FinalizeEntry(pending, byPath, childDirectories, limits)` — Finalizes entry while enforcing the relevant format and safety invariants.
- `private byte [] ReadEntryBytes(source, entry, blockSize, expectedLength)` — Reads entry bytes while enforcing the relevant format and safety invariants.
- `private ParsedDirectoryRecord ParseDirectoryRecord(record, blockSize, sourceLength, volumeSize, context)` — Parses directory record while enforcing the relevant format and safety invariants.
- `private ushort ReadBothEndianUInt16(data, offset, field)` — Reads both endian u int 16 while enforcing the relevant format and safety invariants.
- `private uint ReadBothEndianUInt32(data, offset, field)` — Reads both endian u int 32 while enforcing the relevant format and safety invariants.
- `private bool ContainsUnsupportedIdentifierByte(data)` — Determines whether unsupported identifier byte.
- `private bool ContainsOnlyAsciiDigits(data)` — Determines whether only ASCII digits.
- `private bool ContainsNonAscii(data)` — Determines whether non ASCII.
- `private string DecodePaddedAscii(data)` — Decodes padded ASCII while enforcing the relevant format and safety invariants.
- `private string NormalizeIdentifier(raw)` — Normalizes identifier while enforcing the relevant format and safety invariants.
- `private void ValidatePathComponent(component)` — Validates path component while enforcing the relevant format and safety invariants.
- `private string NormalizeLookupPath(path)` — Normalizes lookup path while enforcing the relevant format and safety invariants.
- `private void RequireExtentLimit(extentCount, limits)` — Requires extent limit while enforcing the relevant format and safety invariants.
- `private void ThrowIfDisposed()` — Throws when disposed.

## `src/LuckyStarPspToolkit.Formats/Iso/Iso9660Models.cs`

### Types

- `public record Iso9660Extent` — Represents immutable ISO 9660 extent data exchanged by the toolkit.
- `internal record Iso9660DirectoryRecordLocation` — Represents immutable ISO 9660 directory record location data exchanged by the toolkit.
- `public class Iso9660Entry` — Represents the toolkit's ISO 9660 entry model or service.
- `public class Iso9660Summary` — Represents the toolkit's ISO 9660 summary model or service.

### Methods and functions

- `public long ByteOffset(logicalBlockSize)` — Converts an ISO logical block number to a checked absolute byte offset.

## `src/LuckyStarPspToolkit.Formats/Iso/Iso9660Rebuilder.cs`

### Types

- `public class Iso9660PatchManifest` — Represents the toolkit's ISO 9660 patch manifest model or service.
- `public class Iso9660PatchManifestEntry` — Represents the toolkit's ISO 9660 patch manifest entry model or service.
- `public record Iso9660ReplacementRequest` — Represents immutable ISO 9660 replacement request data exchanged by the toolkit.
- `public class Iso9660PreparedPatch` — Represents the toolkit's ISO 9660 prepared patch model or service.
- `public class Iso9660ReplacementReport` — Represents the toolkit's ISO 9660 replacement report model or service.
- `public class Iso9660RebuildReport` — Represents the toolkit's ISO 9660 rebuild report model or service.
- `public class Iso9660Rebuilder` — Provides ISO 9660 rebuilder operations with strict bounds and format validation.
- `private record struct ByteRange` — Represents immutable byte range data exchanged by the toolkit.
- `private class PreparedReplacement` — Represents the toolkit's prepared replacement model or service.

### Methods and functions

- `internal constructor Iso9660PreparedPatch(manifestPath, manifestSha256, expectedSourceSha256, replacements)` — Initializes a new instance with validated constructor state.
- `public Iso9660RebuildReport ApplyManifest(sourceIsoPath, manifestPath, outputIsoPath, limits)` — Loads a strict ISO patch manifest and rebuilds a new verified ISO image.
- `public Iso9660RebuildReport ApplyPreparedPatch(sourceIsoPath, patch, outputIsoPath, limits)` — Applies an already validated ISO patch plan to a new output image.
- `public IReadOnlyList < string > ResolveManifestReplacementFiles(manifestPath, limits)` — Resolves manifest replacement files while enforcing the relevant format and safety invariants.
- `public Iso9660PreparedPatch LoadPatchManifest(manifestPath, limits)` — Loads, validates, snapshots, and resolves an ISO patch manifest.
- `public Iso9660RebuildReport Rebuild(sourceIsoPath, replacements, outputIsoPath, expectedSourceSha256, limits)` — Stages a new ISO with authenticated replacements and verifies unchanged data before committing it.
- `private Iso9660RebuildReport RebuildCore(sourceIsoPath, replacements, outputIsoPath, expectedSourceSha256, manifestPath, manifestSha256, limits)` — Rebuilds core while enforcing the relevant format and safety invariants.
- `public string ToHumanText(report)` — Formats the current result as human-readable diagnostic text.
- `private PreparedReplacement [] PrepareReplacements(image, requests, sourcePath, outputPath, manifestPath, limits)` — Prepares replacements while enforcing the relevant format and safety invariants.
- `private void AssignOutputLocations(image, replacements, limits, out outputSize, out outputBlocks)` — Assigns aligned append-only extents and validates the final volume and replacement size limits.
- `private void EnsureRebuildableDescriptorSet(image)` — Ensures rebuildable descriptor set while enforcing the relevant format and safety invariants.
- `private void CopyReplacementToOutput(replacement, output)` — Copies replacement to output while enforcing the relevant format and safety invariants.
- `private void PatchDirectoryRecord(output, replacement)` — Updates both endian copies of a replacement file location and length in its ISO directory record.
- `private void WriteBothEndianUInt32(output, offset, value, context)` — Writes both endian u int 32 while enforcing the relevant format and safety invariants.
- `private void VerifySourcePrefixUnchanged(original, staged, replacements)` — Verifies that ISO bytes outside explicitly mutable metadata ranges remain byte-for-byte unchanged.
- `private ByteRange [] NormalizeMutableRanges(ranges, sourceSize)` — Normalizes mutable ranges while enforcing the relevant format and safety invariants.
- `private void CompareUnchangedRange(original, staged, offset, length, originalBuffer, stagedBuffer)` — Compares an unmodified ISO range through reusable buffers and fails at the first changed byte.
- `private int FirstDifference(left, right)` — Finds the first unequal byte, or the shared-length boundary when only the lengths differ.
- `private void VerifyStagedImage(original, staged, replacements, expectedOutputSize, expectedOutputBlocks)` — Verifies staged image while enforcing the relevant format and safety invariants.
- `private string ComputeStreamSha256(stream)` — Computes stream SHA 256 while enforcing the relevant format and safety invariants.
- `private void PadTo(output, alignment)` — Appends zero bytes until the output position is a multiple of the requested alignment.
- `private string? NormalizeOptionalSha256(value, context)` — Normalizes optional SHA 256 while enforcing the relevant format and safety invariants.
- `private byte [] ReadManifestSnapshot(path, limits)` — Reads manifest snapshot while enforcing the relevant format and safety invariants.

## `src/LuckyStarPspToolkit.Formats/Iso/PspAssetCollector.cs`

### Types

- `public record PspAssetCollectionOptions` — Represents immutable PSP asset collection options data exchanged by the toolkit.
- `public class PspAssetFileReport` — Represents the toolkit's PSP asset file report model or service.
- `public class PspAssetBundleReport` — Represents the toolkit's PSP asset bundle report model or service.
- `public class PspAssetCollector` — Provides the toolkit's PSP asset collector workflow.
- `private record AssetDefinition` — Represents immutable asset definition data exchanged by the toolkit.

### Methods and functions

- `public PspAssetBundleReport CollectFromIso(isoPath, outputZipPath, options, limits)` — Collects the minimum translation assets from a caller-owned PSP ISO into a deterministic ZIP bundle.
- `public string ToHumanText(report)` — Formats the current result as human-readable diagnostic text.
- `private void WriteBundle(image, output, report, options, limits)` — Writes bundle while enforcing the relevant format and safety invariants.
- `private void WriteZipEntry(zip, name, data)` — Writes ZIP entry while enforcing the relevant format and safety invariants.

## `src/LuckyStarPspToolkit.Formats/Iso/SeekableDataSource.cs`

### Types

- `internal class SeekableDataSource` — Represents the toolkit's seekable data source model or service.

### Methods and functions

- `private constructor SeekableDataSource(stream, leaveOpen)` — Initializes a new instance with validated constructor state.
- `public SeekableDataSource OpenFile(path)` — Opens file while enforcing the relevant format and safety invariants.
- `public SeekableDataSource FromMemory(data)` — Creates memory while enforcing the relevant format and safety invariants.
- `public SeekableDataSource FromStream(stream, leaveOpen)` — Creates stream while enforcing the relevant format and safety invariants.
- `public void ReadExactly(offset, destination, context)` — Reads exactly while enforcing the relevant format and safety invariants.
- `public void CopyRangeTo(offset, count, destination, hash)` — Copies range to while enforcing the relevant format and safety invariants.
- `public string ComputeSha256()` — Computes SHA 256 while enforcing the relevant format and safety invariants.
- `public void Dispose()` — Releases owned streams and other disposable resources.

## `src/LuckyStarPspToolkit.Formats/Scripts/LuckyStarScript.cs`

### Types

- `public class LuckyStarScript` — Represents the toolkit's lucky star script model or service.
- `private enum IntervalKind` — Defines the supported interval kind values.
- `private record Interval` — Represents immutable interval data exchanged by the toolkit.
- `private record Replacement` — Represents immutable replacement data exchanged by the toolkit.
- `private record MapSegment` — Represents immutable map segment data exchanged by the toolkit.

### Methods and functions

- `private constructor LuckyStarScript(original, profile, magic, dialogTableAbsolute, choiceTableAbsolute, contentStart, logicalEnd, jumps, dialogs, choiceGroups)` — Initializes a new instance with validated constructor state.
- `public LuckyStarScript Parse(data, profile, limits, requireChecksum)` — Parses validated input into the current binary-format model.
- `public ScriptBuildResult Build(mutation, limits)` — Serializes the current validated model into its binary representation.
- `private void ValidateMutationKeys(mutation)` — Validates mutation keys while enforcing the relevant format and safety invariants.
- `private byte [] BuildDialog(id, speaker, message, terminator)` — Builds dialog while enforcing the relevant format and safety invariants.
- `private byte [] BuildChoice(glyphs)` — Builds choice while enforcing the relevant format and safety invariants.
- `private int MapBoundary(oldOffset, map)` — Relocates a script segment boundary while preserving whether it lies before or after a text replacement.
- `private int MapOffset(oldOffset, map)` — Relocates an original script offset and rejects ambiguous references inside changed text.
- `private ushort [] ReadGlyphsUntil(data, ref position, terminator, limit, context)` — Reads glyphs until while enforcing the relevant format and safety invariants.
- `private ushort ReadUInt16(data, ref position, context)` — Reads u int 16 while enforcing the relevant format and safety invariants.
- `private uint ReadUInt32(data, offset, context)` — Reads u int 32 while enforcing the relevant format and safety invariants.
- `private void WriteUInt16(data, ref position, value)` — Writes u int 16 while enforcing the relevant format and safety invariants.
- `private int RelativeToAbsolute(relative, jumpTableOffset, limit, context)` — Adds the scenario jump-table base to a relative offset using checked arithmetic.
- `private void EnsureRange(data, offset, length, context)` — Ensures range while enforcing the relevant format and safety invariants.
- `private int Blocks2K(length)` — Converts a scenario byte count into the required number of 2048-byte allocation blocks.

## `src/LuckyStarPspToolkit.Formats/Scripts/RgoChecksum.cs`

### Types

- `public class RgoChecksum` — Represents the toolkit's RGO checksum model or service.

### Methods and functions

- `public ( ulong Low , ulong High ) Calculate(data)` — Computes the two wrapping 64-bit sums over all 16-byte blocks before the stored RGO checksum.
- `public bool Verify(data)` — Compares the stored 16-byte RGO checksum with a fresh calculation over the preceding file bytes.
- `public void Apply(data)` — Applies the requested transformation after validating all preconditions.

## `src/LuckyStarPspToolkit.Formats/Scripts/ScriptModels.cs`

### Types

- `public enum LuckyStarGame` — Defines the supported lucky star game values.
- `public record ScriptProfile` — Represents immutable script profile data exchanged by the toolkit.
- `public class ScriptDialog` — Represents the toolkit's script dialog model or service.
- `public class ScriptChoice` — Represents the toolkit's script choice model or service.
- `public class ScriptChoiceGroup` — Represents the toolkit's script choice group model or service.
- `public class ScriptMutation` — Represents the toolkit's script mutation model or service.
- `public record ScriptBuildResult` — Represents immutable script build result data exchanged by the toolkit.

### Methods and functions

- `public ScriptProfile Parse(value)` — Parses validated input into the current binary-format model.

## `src/LuckyStarPspToolkit.Formats/Text/GlyphMap.cs`

### Types

- `public record GlyphDuplicate` — Represents immutable glyph duplicate data exchanged by the toolkit.
- `public record GlyphMapAnalysis` — Represents immutable glyph map analysis data exchanged by the toolkit.
- `public class GlyphMap` — Represents the toolkit's glyph map model or service.
- `private class TrieNode` — Represents the toolkit's trie node model or service.

### Methods and functions

- `private constructor GlyphMap(glyphs, sourceSha256)` — Initializes a new instance with validated constructor state.
- `public string GetText(index)` — Gets text while enforcing the relevant format and safety invariants.
- `public bool TryGetLowestIndex(text, out index)` — Attempts to get lowest index.
- `public IEnumerable < ( ushort Index , string Text ) > EnumerateEncodableEntries()` — Enumerates nonempty glyph-map entries below the reserved control-code range, in index order.
- `public GlyphMapAnalysis Analyze()` — Analyzes the supplied model and returns deterministic diagnostics.
- `public GlyphMap Load(path, limits)` — Loads and validates the requested data from its source.
- `public GlyphMap FromUtf8(data, limits)` — Parses one immutable UTF-8 snapshot and hashes the same bytes used for decoding.
- `public GlyphMap FromLines(lines, limits)` — Creates lines while enforcing the relevant format and safety invariants.
- `private void ValidateTextBudget(text, limits)` — Rejects excessive lines or label lengths before allocating a split array or trie nodes.
- `public string Decode(indices)` — Decodes validated input into the source representation.
- `public ushort [] Encode(text)` — Encodes validated text or binary state into the target representation.
- `private bool TryMatch(text, position, out index, out consumed)` — Attempts to match.
- `private bool TryConsumeToken(text, ref position, output)` — Attempts to consume token.

## `src/LuckyStarPspToolkit.Formats/Workspace/EbootSizePatchPlan.cs`

### Types

- `public class EbootSizePatchPlan` — Represents the toolkit's EBOOT size patch plan model or service.
- `internal class RgoScriptHeapPatcher` — Provides RGO script heap patcher operations with strict bounds and format validation.
- `public class EbootSizePatchEntry` — Represents the toolkit's EBOOT size patch entry model or service.
- `internal class EbootSizeTablePatcher` — Provides EBOOT size table patcher operations with strict bounds and format validation.

### Methods and functions

- `public EbootSizePatchPlan Create(profile, entries, sourceCpkSha256, rebuiltCpkSha256)` — Creates and validates a game-specific EBOOT size plan with sorted script entries and optional CPK hashes.
- `public void Validate()` — Validates the supplied state and rejects violated format invariants.
- `public byte [] Apply(decryptedEboot, allowUnknownHash)` — Applies the requested transformation after validating all preconditions.
- `public EbootSizePatchPlan Load(path, limits)` — Loads and validates the requested data from its source.
- `public void Save(path)` — Persists the validated data to its destination.
- `private void ValidateCanonicalLayout(game)` — Validates canonical layout while enforcing the relevant format and safety invariants.
- `private void ValidateOptionalHash(hash, code, name)` — Validates optional hash while enforcing the relevant format and safety invariants.
- `internal void EnsureCapacity(eboot, tableOffset, firstId, lastIdInclusive)` — Ensures capacity while enforcing the relevant format and safety invariants.
- `internal int DecodeCapacity(luiInstruction, addiuInstruction)` — Decodes capacity while enforcing the relevant format and safety invariants.
- `internal ( uint Lui , uint Addiu ) EncodeCapacity(bytes)` — Encodes capacity while enforcing the relevant format and safety invariants.
- `internal byte [] Apply(source, tableOffset, firstId, lastIdInclusive, entries)` — Applies the requested transformation after validating all preconditions.

## `src/LuckyStarPspToolkit.Formats/Workspace/TranslationWorkspaceService.cs`

### Types

- `public class TranslationWorkspaceService` — Provides the toolkit's translation workspace service workflow.
- `private record PreparedWorkspaceBuild` — Represents immutable prepared workspace build data exchanged by the toolkit.

### Methods and functions

- `public WorkspaceExportResult Export(sourceCpkPath, glyphMapPath, outputDirectory, profile, scriptIds, limits)` — Creates an isolated translation workspace from authenticated CPK bytes and a single glyph-map snapshot.
- `public WorkspaceValidationResult Validate(workspaceDirectory, sourceCpkPath, limits)` — Validates the supplied state and rejects violated format invariants.
- `public WorkspaceBuildResult Build(workspaceDirectory, sourceCpkPath, outputCpkPath, outputPlanPath, limits)` — Serializes the current validated model into its binary representation.
- `private PreparedWorkspaceBuild PrepareBuild(workspaceDirectory, sourceCpkPath, limits)` — Prepares build while enforcing the relevant format and safety invariants.
- `private void ValidateManifest(manifest)` — Validates manifest while enforcing the relevant format and safety invariants.
- `private void ValidateSha256(value, code, name)` — Validates SHA 256 while enforcing the relevant format and safety invariants.
- `private TranslationScriptFile ToWorkspaceFile(scriptId, script, map, source)` — Converts workspace file while enforcing the relevant format and safety invariants.
- `private ScriptMutation BuildMutation(script, translation, map)` — Builds mutation while enforcing the relevant format and safety invariants.
- `private void ValidateStructuralGlyphs(glyphs, forbidden, context)` — Validates structural glyphs while enforcing the relevant format and safety invariants.
- `private void ValidateWorkspaceScript(reference, script)` — Validates workspace script while enforcing the relevant format and safety invariants.
- `private List < ushort > ResolveScriptIds(archive, profile, requested)` — Resolves script ids while enforcing the relevant format and safety invariants.
- `private T ReadJson<T>(path, limits)` — Reads JSON while enforcing the relevant format and safety invariants.
- `private void WriteJson<T>(path, value)` — Writes JSON while enforcing the relevant format and safety invariants.

## `src/LuckyStarPspToolkit.Formats/Workspace/WorkspaceModels.cs`

### Types

- `public class TranslationWorkspaceManifest` — Represents the toolkit's translation workspace manifest model or service.
- `public class TranslationWorkspaceScriptReference` — Represents the toolkit's translation workspace script reference model or service.
- `public class TranslationScriptFile` — Represents the toolkit's translation script file model or service.
- `public class TranslationDialog` — Represents the toolkit's translation dialog model or service.
- `public class TranslationChoiceGroup` — Represents the toolkit's translation choice group model or service.
- `public class TranslationChoice` — Represents the toolkit's translation choice model or service.
- `public record WorkspaceExportResult` — Represents immutable workspace export result data exchanged by the toolkit.
- `public record WorkspaceScriptValidationResult` — Represents immutable workspace script validation result data exchanged by the toolkit.
- `public record WorkspaceValidationResult` — Represents immutable workspace validation result data exchanged by the toolkit.
- `public record WorkspaceBuildResult` — Represents immutable workspace build result data exchanged by the toolkit.

## Coverage

- Runtime C# files: 44
- Named types: 156
- Named methods, constructors, and local functions: 448

Test-source documentation is validated as part of the release gate but omitted from this public API catalog.
